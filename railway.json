{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npx prisma generate && npx prisma migrate deploy && npx prisma db seed"
  },
  "deploy": {
    "startCommand": "node src/index.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
