{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npx prisma generate"
  },
  "deploy": {
    "startCommand": "npx prisma migrate deploy && npx prisma db seed && (node scripts/backfill-water-steps-goals.js || true) && (node scripts/assign-todays-tasks-now.js || true) && node src/index.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
